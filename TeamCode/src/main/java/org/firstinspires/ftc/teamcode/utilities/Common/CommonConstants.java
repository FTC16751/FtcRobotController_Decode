package org.firstinspires.ftc.teamcode.utilities.Common;

public class CommonConstants {

    private CommonConstants() {}
    /**
     * A canonical, project-wide enum to represent the team alliance.
     * This serves as the Single Source of Truth for alliance color
     * to prevent type conflicts between different subsystems.
     */
    public enum Alliance {
        RED,
        BLUE;
    }
}
